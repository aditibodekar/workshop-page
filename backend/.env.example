# Port the Express server listens on
PORT=5000

# Comma-separated list of allowed CORS origins (your frontend URLs)
CLIENT_ORIGIN=http://localhost:5173,https://your-frontend.vercel.app

# MongoDB connection string. Leave blank to run without a database
# (the API will still work and log enquiries, but won't persist them).
MONGODB_URI=mongodb+srv://aditibodekar05_db_user:cojuMCKOY7Uaj8Tc@form.yttb3g9.mongodb.net/workshopdb

# Node environment
NODE_ENV=development
